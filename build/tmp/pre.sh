#!/bin/bash
# pre.sh — Pre-build setup script for OpenResty + Tongsuo
# This runs before downloading and building components.
# Add any pre-build steps here (e.g., installing extra build deps).
echo "[pre.sh] Pre-build setup complete."
